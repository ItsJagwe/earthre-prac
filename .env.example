# Client-safe. These are exposed to the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Do not put SUPABASE_SERVICE_ROLE_KEY here. Hosted Edge Functions receive it
# automatically and it must never be sent to the browser.
